// Test stub for the `server-only` package, which throws when imported outside a
// React Server Component. Aliased in vitest.config.ts so server modules import
// cleanly under the Node test runner.
export {};
